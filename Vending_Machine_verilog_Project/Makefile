TOPLEVEL_LANG = verilog
VERILOG_SOURCES = $(shell pwd)/vending_machine.v
TOPLEVEL = vending_machine
MODULE = test_vending_machine
SIM = icarus

include $(shell cocotb-config --makefiles)/Makefile.sim
